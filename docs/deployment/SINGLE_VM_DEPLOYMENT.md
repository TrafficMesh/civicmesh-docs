# Single VM Deployment

Requirements: Ubuntu 20.04, 4vCPU, 8GB RAM, 100GB storage

Setup:
1. Install Docker
2. Clone civicmesh-backend
3. Configure environment
4. Run docker compose
5. Configure Nginx + TLS
